FROM docker.io/library/node:latest

WORKDIR /design_of_www
COPY package*.json ./
RUN npm ci \
    && npm install -g serve \
    && npm cache clean --force

COPY ./ /design_of_www

RUN npm run build
EXPOSE 3000

CMD ["serve", "-s", "build"]
